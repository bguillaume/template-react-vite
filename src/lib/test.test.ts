import { describe, it, expect } from 'vitest'

describe(`'sanity test', () => {
  it(`'should pass', () => {
    expect(true).toBe(true)
  })
  it(`'should handle math', () => {
    expect(1 + 1).toBe(2)
  })
})
