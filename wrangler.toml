# === Cloudflare Pages config (read by Pages auto-build + `wrangler pages deploy`) ===
# Workers Builds uses wrangler.workers.toml instead (passed via -c flag in deploy_command).
name = "template-react-vite"
compatibility_date = "2024-09-23"
pages_build_output_dir = "./dist"

[vars]
ENVIRONMENT = "production"
